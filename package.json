{
  "name": "autopilot-marketplace-docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "docs:dev": "npm run docs:generate && vitepress dev docs",
    "docs:build": "npm run docs:generate && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:generate": "node scripts/generate-dmms-docs.mjs",
    "dmms:migrate": "node scripts/migrate-dmms-hierarchy.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vite": "^7.3.1"
  }
}
